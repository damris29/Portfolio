import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/ThemeProvider";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Your Name | CS Student & Software Engineer",
  description: "Computer Science student passionate about software engineering, AI, and building innovative solutions. View my projects, skills, and experience.",
  keywords: ["software engineer", "computer science", "portfolio", "developer", "AI", "machine learning"],
  authors: [{ name: "Your Name" }],
  openGraph: {
    title: "Your Name | CS Student & Software Engineer",
    description: "Computer Science student passionate about software engineering, AI, and building innovative solutions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider>
          <Header />
          <main>{children}</main>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}
